package Model;

/** The VerifyTime functional interface. This is the interface used for my lambda expression in the AppointmentController
 *  class. */
public interface VerifyTime {
    boolean isTime(String t);
}
